Tifffile saves tiffs correctly -> Gimp can't open float32, if you want to view them store them in float16.


Remember that format of tensors always starts with batch size -> first index is batch index.